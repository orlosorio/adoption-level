/**
 * Replace before launch (Accionables quiz spec):
 * - BEEHIIV_ENDPOINT — Beehiiv form/API URL for email capture (embed or POST target)
 * - NEWSLETTER_URL — Public Beehiiv (or other) newsletter subscribe page for the CTA button
 * - OG_IMAGE_URL — Absolute URL to 1200×630 Open Graph image (also referenced in app/layout.tsx)
 */
export const BEEHIIV_ENDPOINT = 'YOUR_BEEHIIV_ENDPOINT';

export const NEWSLETTER_URL = 'YOUR_NEWSLETTER_URL';

export const OG_IMAGE_URL = 'YOUR_OG_IMAGE_URL';
